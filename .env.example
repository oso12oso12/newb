# Copy this to a real .env for local testing, or set these directly in
# Railway's Variables tab for the deployed app. Never commit real secrets.

SECRET_KEY=replace-with-a-long-random-string
DEBUG=False
ALLOWED_HOSTS=
# DATABASE_URL is set automatically by Railway when you attach a Postgres
# plugin — you normally don't need to set this yourself.
DATABASE_URL=
